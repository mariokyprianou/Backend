type TimeZone {
  id: ID!
  timeZone: String!
}

input TimeZoneFilter {
  id: ID
  ids: [ID!]
}

extend type Query {
  TimeZone(id: ID!): TimeZone
  allTimeZones(
    page: Int
    perPage: Int
    sortField: String
    sortOrder: String
    filter: TimeZoneFilter
  ): [TimeZone!]!
}
