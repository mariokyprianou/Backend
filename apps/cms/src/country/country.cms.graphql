type Country {
  id: ID!
  country: String!
  regions: [Region!]!
}

type Region {
  id: ID!
  region: String!
}

# input CountryFilter {
#   id: ID
#   ids: [ID!]
# }

extend type Query {
  Country(id: ID!): Country
}
